{% extends 'admin.html.twig' %}

{% block body %}
    <h1>Showorder</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ showOrder.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('showorder_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('showorder_edit', { 'id': showOrder.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
