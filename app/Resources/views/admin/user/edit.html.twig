{% extends 'admin/admin.html.twig' %}

{% block body %}

    <div class="row">

        <div class="col-sm-12">

            <h1>User edit</h1>

        </div>

    </div>

    <div class="row">

        <div class="col-sm-8">

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input class="btn btn-primary" type="submit" value="Edit" />
            <a class="btn btn-default" href="{{ path('user_index') }}">Cancel</a>
            {{ form_end(edit_form) }}

        </div>

        <div class="col-sm-4">

            <a href="{{ path('user_change_password', {id: user.id}) }}">Change Password</a>

        </div>

    </div>

{% endblock %}
