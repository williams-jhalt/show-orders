{% extends 'admin/admin.html.twig' %}

{% block body %}

    <h1>Vendor edit</h1>

    <img src="{{ asset('uploads/vendors/' ~ existing_image) }}">    
    
    {{ form_start(edit_form) }}
    
    {{ form_widget(edit_form) }}

    <input class="btn btn-primary" type="submit" value="Update" />
    <a class="btn btn-default" href="{{ path('vendor_index') }}">Cancel</a>

    {{ form_end(edit_form) }}

    <br>

    <p><a class="btn btn-default" href="{{ path('product_import', {vendorId: vendor.id}) }}">Import Products</a></p>

{% endblock %}
